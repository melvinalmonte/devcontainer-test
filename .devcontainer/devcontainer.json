{
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": [
    "--device=/dev/fuse",
    "--cap-add=SYS_ADMIN",
    "--cap-add=NET_ADMIN",
    "--cap-add=SETUID",
    "--cap-add=SETGID",
    "--security-opt",
    "seccomp=unconfined",
    "--security-opt",
    "apparmor=unconfined",
    "--tmpfs",
    "/tmp:exec,suid,size=2g",
    "--tmpfs",
    "/run:exec,suid,size=2g",
    "--tmpfs",
    "/sys/fs/cgroup:exec,suid,size=100m"
  ],
  "containerEnv": {
    "TMPDIR": "/tmp"
  }
}